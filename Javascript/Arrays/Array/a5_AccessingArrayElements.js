/* 
Accessing Array elements
===========================

*/

var elements=["vikas", "ravi"];
console.log(elements[0]); // vikas  ...accessing array element
console.log(elements[3]) ; // undefined....because elements[3] values does not exist.


// Accessing array elements all at once

console.log(elements); // [ 'vikas', 'ravi' ] .. if we write like this we can access all array elements at once.

