/* 
    replace function
    ======================

    // replace(old, new ) Method
    // The replace() method replaces a specified value with another value in a string, 
    replace() function replaces only the first match.
*/

var elements='vikas    gautam';
elements.replace('gautam' ,'vikas');
console.log(elements); //vikas    gautam
