/* 
CREATING DATE OBJECT
========================

new Date( ) - new Date() creates a new date object with the current date and time.

Ex: - 

*/

// CREATE OBJECT AND GET THE CURRENT TIME.
    var tarikh = new Date( );
    document.write(tarikh);

/* 
new Date(milliseconds) – It creates a new date object as January 1, 1970, 00:00:00 
Universal Time (UTC).
Ex:
*/
    let date1 = new Date(8640000);	
    document.write(date1);
