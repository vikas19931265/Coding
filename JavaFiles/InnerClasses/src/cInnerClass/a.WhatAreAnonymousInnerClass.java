package cInnerClass;

/*

Anonymous Inner classes(Inner class without name)
---------------------------

1. Sometimes we can declare inner class without name. Such type of inner classes are called as 
   Anonymous inner classes.

2. The main purpose of anonymous inner classes is just for instant use( only one time usage).


Based upon declaration and behavior there are three types of anonymous inner classes.

1. Anonymous inner class that extends a class.

2. Anonymous inner that implements an interface.

3. Anonymous inner that defined inside arguments.


*/
public class NewClass15 {
    
}

