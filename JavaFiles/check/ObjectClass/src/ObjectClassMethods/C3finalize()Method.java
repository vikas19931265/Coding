/*
finalize() method
==========================

1. Just before destroying an object, garbage collector will  call finalize method to 
   perform clean up activities.

2. Once finalize method completes , automatically garbage collector destroys that object.


3. finalize() method , will be called by the JVM on an object.


*/
