/*

Difference between enum vs Enum vs Enumeration
=========================================================================

enum
========

enum is the keyword in java which can be used to define a group of named constants.

Enum is the class in java present in java.lang package. Every enum in java should be
direct child class of Enum class hence this class acts as base class for all the java
Enum.


Enumeration is an interface present inside java.util package.

We can use enumeration object to get objects one by one from the collections.


*/
