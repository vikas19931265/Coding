package com.studentcrud.dto;
import com.studentcrud.model.StudentModel;

import ch.qos.logback.core.joran.util.beans.BeanUtil;

import com.studentcrud.entity.*;


public class StudentDTO {

	
	/*public  Student  dtoToEntityConvertor(StudentModel studModel)
	{
		
		
		 * Student student = new Student();
		 * 
		 * student.setAddress(studModel.getAddress());
		 * student.setEmail(studModel.getEmail());
		 * student.setGender(studModel.getGender());
		 * student.setName(studModel.getName());
		 * 
		 * BeanUtil student.getAddress()
		 * 
		 * return null;
		 */
	}



