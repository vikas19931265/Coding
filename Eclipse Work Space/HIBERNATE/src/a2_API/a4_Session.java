/*
Session
=================

It is an interface defined in org.hibernate package.

It acts as the main runtime interface between java appliation and hibernate.

By using session we can perform DB operations(CURD).

Session object means, it is an object of class which implements Session interface.(SessionImpl).

From the sessionfactory we will get the session objects.

Session inteface provides multiple methods to perform DB operations.

which are

save(), persist(), saveOrUpdate(),update(), merge(), delete(), load(), get() and so on....

*/